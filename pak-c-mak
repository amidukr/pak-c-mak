#!/bin/bash
set -e

COMMAND=$1; shift

export PAK_C_MAK_PATH=$(realpath "$(dirname "$BASH_SOURCE")")

COMMAND_SCRIPT="$PAK_C_MAK_PATH/commands/pak-c-mak-$COMMAND"

if [ -f "$COMMAND_SCRIPT" ]; then
    "$COMMAND_SCRIPT" $@
else
    "$PAK_C_MAK_PATH/commands/pak-c-mak-help"
	exit 1;
fi
